[pytest]
testpaths = tests
norecursedirs = backup .worktrees dist build audio e2e manual
markers =
    live_db: tests that require a configured MySQL database and are excluded from default collection
    external_service: tests that require a deployed service, browser binary, or external credential
